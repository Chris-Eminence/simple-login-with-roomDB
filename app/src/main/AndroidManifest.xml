<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.contactapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContactApp">
        <activity android:name=".Login"></activity>
        <activity android:name=".SignUp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".add_contact_dialogue"></activity> -->
        <activity
            android:name=".MentorsCategory"
            android:label="Mentors" />
        <activity
            android:name=".WorkCategory"
            android:label="Work " />
        <activity
            android:name=".FamilyCategory"
            android:label="Family " />
        <activity
            android:name=".FriendsCategory"
            android:label="Friends" />
        <activity
            android:name=".MainActivity"
            android:label="Contacts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>